#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    int max;
    cin >> max;
    int temp;
    cin >> temp;
    if (temp > max)
    {
        cout << temp << endl;
    }
    else cout << max << endl;
    
    return 0;
}
