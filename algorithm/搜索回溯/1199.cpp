#include <iostream>
#include <cstring>
using namespace std;
string str;
// char res[7];
// int size = -1;

void dfs(int idx,string res){
    if(idx == str.length()){
        cout << res << endl;
        return;
    }
    for(int i = idx;i < res.length();i++){
        
    }
}

int main(){
    cin >> str;
    return 0;
}